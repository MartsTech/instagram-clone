const HeaderProfile = () => {
  return (
    <img
      loading="lazy"
      src=""
      alt="profile"
      className="h-10 rounded-full cursor-pointer"
    />
  );
};

export default HeaderProfile;
